To run the files with standard build flow, do
python run_batch.py

To run the files with custom build flow, do
python run_batch_custom.py

the build.py and custom steps file are in the dataflow_build_dir_custom folder
