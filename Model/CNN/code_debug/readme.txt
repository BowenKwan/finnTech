To run the files with normal build flow, do
python run_batch.py

To run the files with custom build flow, do
python run_batch_custom.py

the custom build file and custom steps file are in dataflow_build_dir_custom folder
